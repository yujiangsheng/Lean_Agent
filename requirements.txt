# ═══════════════════════════════════════════════════════════════════════════
#            Gauss v3.0.0 - Python 依赖 (Python Dependencies)
# ═══════════════════════════════════════════════════════════════════════════
#
# 分层依赖 (Layered Dependencies):
#   - 核心层 (Core):   numpy, requests — Mock 模式即可运行
#   - Lean 层 (Lean):   lean-dojo — 连接 Lean 4 进行形式化验证
#   - LLM 层 (LLM):    transformers, torch — 本地模型推理
#   - 嵌入层 (Embed):  sentence-transformers, networkx — 语义搜索与图分析
#   - 工具层 (Tools):  tqdm — 进度条
#
# 安装命令 (Install):
#   pip install -r requirements.txt           # 安装全部依赖
#   pip install numpy requests                # 仅安装核心依赖 (Mock 模式)
#
# 注意 (Notes):
#   - 如果只使用 Ollama API 调用 LLM，torch/transformers 可以跳过
#   - lean-dojo 需要已安装 Lean 4 工具链
#   - 推荐使用 Python 3.9+
#
# ═══════════════════════════════════════════════════════════════════════════

# ─────────────────────────────────────────────────────────────────────────────
# 核心依赖 (Core — required for all modes)
# ─────────────────────────────────────────────────────────────────────────────
numpy>=1.24.0
requests>=2.28.0

# ─────────────────────────────────────────────────────────────────────────────
# LeanDojo (Lean 4 交互 — optional, for formal verification)
# ─────────────────────────────────────────────────────────────────────────────
lean-dojo>=1.8.0

# ─────────────────────────────────────────────────────────────────────────────
# LLM 相关 (LLM — optional, for local model inference)
# 如果仅使用 Ollama API，可跳过此部分
# ─────────────────────────────────────────────────────────────────────────────
transformers>=4.35.0
torch>=2.0.0
accelerate>=0.24.0
sentencepiece>=0.1.99

# ─────────────────────────────────────────────────────────────────────────────
# 嵌入与图分析 (Embeddings & Graph — for semantic search)
# ─────────────────────────────────────────────────────────────────────────────
sentence-transformers>=2.2.0
networkx>=3.0
scikit-learn>=1.3.0

# ─────────────────────────────────────────────────────────────────────────────
# 工具 (Utilities)
# ─────────────────────────────────────────────────────────────────────────────
tqdm>=4.65.0
